<?php

declare(strict_types=1);

namespace VoiceML\Model;

/**
 * Marker interface for every response model exposed by the SDK.
 *
 * Implementations should be plain DTOs with readonly properties and a
 * `public static function fromArray(array $data): self` factory.
 */
interface Model
{
}
